# Homepage illustrations (placeholders)

These images are used by the homepage **Section A — Learning journey** fan-cards
in `index.html` (`.fan__media img`).

`learn-see.jpg`, `learn-act.jpg`, `learn-reflect.jpg` are **temporary
placeholders** copied from the food-target source images. Replace them with
bespoke PlateWise artwork (same filenames) when ready — keep them roughly
16:10 and optimised for web. No code changes are needed after replacing.
